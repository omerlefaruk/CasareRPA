# Skills Index

```xml
<skills_index>
  <!-- CasareRPA skills. Progressive disclosure: SKILL.md loaded first. -->

  <skills>
    <skill name="workflow-creator">Generate workflows from natural language</skill>
    <skill name="test-generator">Generate pytest test suites</skill>
    <skill name="node-template-generator">Generate new node boilerplate</skill>
    <skill name="workflow-validator">Validate workflow JSON</skill>
    <skill name="rpa-patterns">Retry, polling, circuit breaker</skill>
    <skill name="error-recovery">RPA error handling patterns</skill>
    <skill name="selector-strategies">CSS/XPath best practices</skill>
    <skill name="mcp-server">MCP server development</skill>
    <skill name="playwright-testing">Test browser nodes</skill>
    <skill name="parallel-exec">Parallel agent execution</skill>
    <skill name="brain-updater">Update .brain/ context files</skill>
    <skill name="chain-tester">Node chain testing templates</skill>
    <skill name="changelog-updater">Keep a Changelog format</skill>
    <skill name="code-reviewer">Structured code review format</skill>
    <skill name="import-fixer">Fix imports for clean architecture</skill>
    <skill name="agent-invoker">Agent invocation reference</skill>
  </skills>

  <structure>
    <component>SKILL.md</component>     <desc>Entry point (~100 lines)</desc>
    <component>references/</component>  <desc>Loaded on demand</desc>
    <component>examples/</component>     <desc>Concrete samples</desc>
    <component>templates/</component>    <desc>Code templates</desc>
  </structure>
</skills_index>
```

*Token-optimized: All references are lazy-loaded*
